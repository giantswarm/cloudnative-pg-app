name: cloudnative-pg

image:
  registry: gsoci.azurecr.io
  name: giantswarm/cloudnative-pg
  tag: v0.0.1
  pullPolicy: IfNotPresent

cloudnative-pg:
  resources:
    limits:
      cpu: 100m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 200Mi

vpa:
  enabled: true
  containerPolicies: {}
